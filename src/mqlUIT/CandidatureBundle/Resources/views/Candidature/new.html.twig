{% extends '::base.html.twig' %}
  {% block titreP -%} Candidature creation {% endblock %}
{% block body -%}


    <form action="{{ path('candidature_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('candidature') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
