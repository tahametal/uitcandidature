{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Candidature</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Isvalid</th>
                <td>{{ entity.isvalid }}</td>
            </tr>
            <tr>
                <th>Intitulé</th>
                <td>{{ entity. }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('candidature') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('candidature_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('candidature_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}
