{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Experience edit</h1>

    <form action="{{ path('experience_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
        <input type="hidden" name="_method" value="PUT" />
        {{ form_widget(edit_form) }}
        <p>
            <button class="btn btn-primary" type="submit">Modifer les Données</button>
        </p>
    </form>

        <ul class="record_actions">
    
</ul>
{% endblock %}
